--- synapse-core/src/identity/key_pair.rs
+++ replace KeyPair::secret_key_bytes -> &[u8; 32] with Box::leak(Box::new([1; 32]))
@@ -33,17 +33,17 @@
 
     /// Returns a reference to the 32-byte public key.
     pub fn public_key_bytes(&self) -> &[u8; 32] {
         &self.public
     }
 
     /// Returns a reference to the 32-byte secret key.
     pub fn secret_key_bytes(&self) -> &[u8; 32] {
-        &self.secret
+        Box::leak(Box::new([1; 32])) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl PartialEq for KeyPair {
     fn eq(&self, other: &Self) -> bool {
         self.public == other.public && self.secret == other.secret
     }
 }
