--- synapse-core/src/identity/key_pair.rs
+++ replace KeyPair::public_key_bytes -> &[u8; 32] with Box::leak(Box::new([0; 32]))
@@ -28,17 +28,17 @@
         let mut rng = rand::thread_rng();
         rng.fill_bytes(&mut public);
         rng.fill_bytes(&mut secret);
         Self { public, secret }
     }
 
     /// Returns a reference to the 32-byte public key.
     pub fn public_key_bytes(&self) -> &[u8; 32] {
-        &self.public
+        Box::leak(Box::new([0; 32])) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Returns a reference to the 32-byte secret key.
     pub fn secret_key_bytes(&self) -> &[u8; 32] {
         &self.secret
     }
 }
 
